import 'package:flutter/material.dart';

class DiscardPile extends StatelessWidget {
  const DiscardPile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
